# 데이터 처리 및 예외 규칙 v1.0

> 표준 사전에서 정의한 컬럼을 실제로 어떻게 정제·변환할지 규칙화한다.

## 1. 문자열 정규화

### 작성 예시

| Rule ID | 대상 | 원천 예시 | 처리 규칙 | 결과 예시 | 예외 처리 |
|---|---|---|---|---|---|
| RULE-001 | 직원 ID | `emp000038`, `EMP 001921` | 공백 제거 + 대문자 통일 | `EMP000038`, `EMP001921` | 패턴 불일치 시 격리 |
| RULE-002 | 문자열 | `" 김수아 "`, `　팀장　` | 앞뒤 ASCII/Unicode 공백 제거 | `김수아`, `팀장` | 중간 공백은 별도 판단 |

### 팀 작성

| Rule ID | 대상 | 원천 예시 | 처리 규칙 | 결과 예시 | 예외 처리 |
|---|---|---|---|---|---|
|  |  |  |  |  |  |

## 2. 식별자 정규화

### 작성 예시
- `BIZ-31536`, `BIZ_38290`, `BIZ 17549` → 팀 표준 형식으로 통일
- `EMP 001921`, `emp000038` → 직원 ID 표준 규칙 적용

팀 결정:
- 직원 ID 규칙:
- 영역 ID 규칙:
- 패턴 불일치 시 처리:

## 3. 날짜/일시 처리

### 작성 예시

| 원천값 | 해석 | 표준 결과 |
|---|---|---|
| `2026/05/12` | 날짜 | `2026-05-12` |
| `2018.01.18` | 날짜 | `2018-01-18` |
| `20170426002747` | 일시 후보 | 팀 확인 후 표준 포맷 |

팀 결정:
- 표준 날짜 포맷:
- 표준 일시 포맷:
- 시간대:
- 파싱 실패 시 처리:

## 4. Boolean 처리

### 작성 예시

| 원천값 | 표준값 |
|---|---|
| `Y`, `YES`, `1` | true |
| `N`, `NO`, `0` | false |

> `재직`, `사용` 등 한국어 표현은 실제 업무 의미 확인 후 매핑한다.

팀 결정:
- true 매핑값:
- false 매핑값:
- 미정의 값 처리:

## 5. 도메인 매핑

### 작성 예시

`TOP_LEVEL`, `TOP LEVEL`, `top_level`, `최상위`, `L1`, `1`

확인할 것:
1. 실제 같은 의미인지
2. 같다면 어떤 표준값으로 통일할지
3. 다르면 어떻게 분리할지
4. 정의되지 않은 값은 어떻게 처리할지

| 원천값 | 표준값 | 근거 |
|---|---|---|
|  |  |  |

## 6. 결측 처리

### 작성 예시

| 컬럼 | NULL 허용 | 처리 |
|---|---|---|
| `employee_id` | N | 격리 |
| `parent_area_id` | Y 가능 | 최상위 영역이면 허용 가능 |

### 팀 작성

| 컬럼 | NULL 허용 | 처리 | 근거 |
|---|---|---|---|
|  |  |  |  |

## 7. `REG_DT` 처리

> 가이드상 1,000건 값 차이는 임의 선택 금지.

- 기준 소스:
- 기준 시점:
- 선택 근거:
- 변환 전 불일치:
- 변환 후 불일치:
- 회귀 검증 방법:

## 8. Quarantine — 격리 규칙

Quarantine은 **품질 기준을 통과하지 못한 데이터를 정상 데이터와 분리하는 영역**이다.

### 작성 예시

| 오류 코드 | 조건 | 저장 내용 | 재처리 방식 |
|---|---|---|---|
| Q001 | PK 누락 | 원본값 + 오류 사유 | 원천 확인 후 재처리 |
| Q002 | 날짜 파싱 실패 | 원본 날짜 + 실패 포맷 | 규칙 추가 후 재처리 |

### 팀 작성

| 오류 코드 | 조건 | 저장 내용 | 재처리 방식 |
|---|---|---|---|
|  |  |  |  |
